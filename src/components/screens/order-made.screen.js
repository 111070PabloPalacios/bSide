import React from "react";
import {View, Text} from "react-native"

export const OrderMadeScreen = () => {
    return(
        <View style={{alignItems: 'center', justifyContent: 'center'}}>
            <Text>OrderMadeScreen</Text>
        </View>
    );
}